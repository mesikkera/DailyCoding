객체(Object): 실세계에서 정의할 수 있는 모든 형태이며, '속성(data)'과 '기능(action)'을 포함한다. 

	속성: 객체가 가진 상태 및 특성
	기능: 객체가 할 수 있는 행동

	ADT(Abstract Data Type): 객체와 속성, 기능을 도출하는 과정은 일종의 모델링이고, 추상화를 통한 설계 단계
		- 상위 수준의 명세서이므로 코드를 이해하기 쉽게 한다. 
		- 구현 전에 충분히 검증할 수 있으며 쉽게 변경할 수 있다.
		- 추상적인 기술이므로 다른 프로젝트에 재사용할 수 있다. 

